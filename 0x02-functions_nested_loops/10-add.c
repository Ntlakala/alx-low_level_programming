#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first number to be added to b
 * @b: second number to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
